package view.page.utility;

/**
 * Utility Pair.
 * 
 * @param <A>       a generic type.
 * @param <B>       a generic type.
 * @param first     a A object.
 * @param second    a B object.
 * 
 * @author Filippo Gaggi
 */
public record Pair<A, B>(A first, B second) {
}
